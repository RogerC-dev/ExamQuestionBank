# Development & testing requirements for ExamQuestionBank
# This file installs runtime deps plus tools and optional packages used during development.

-r requirements.txt

# Development tools & debugging
django-debug-toolbar>=6.0.0
pipreqs>=0.4.12
pipdeptree>=2.9.0

# Background tasks / caching (optional for development only)
django-redis>=6.0.0
celery>=5.5.3
django-celery-beat>=2.8.1

# Optional auth / integrations (kept in dev to avoid forcing production installs)
django-allauth>=65.11.2
sentry-sdk>=2.39.0

# i18n / translations
django-modeltranslation>=0.19.17

# Storage / image processing
django-storages>=1.14
Pillow>=10.4.0

# Vector / ML extras
faiss-cpu>=1.7.4

# Testing
hypothesis>=6.115.0
pytest>=7.0
pytest-django>=4.5

# Useful REPL
ipython>=8.0
